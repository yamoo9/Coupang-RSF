import { oneOf } from 'prop-types'

/* -------------------------------------------------------------------------- */

// 참고: React에서 null을 oneOfType()에 설정하려면 다음과 같이 사용해야 합니다.
export const nullType = oneOf([null])
